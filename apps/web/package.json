{
  "name": "web",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "check": "biome check . && tsc --noEmit && bun run validate:event-schemas",
    "validate:event-schemas": "bun scripts/validate-event-schemas.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.9",
    "@types/bun": "^1.3.14",
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "@bufbuild/protobuf": "^2.14.0",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1"
  }
}
